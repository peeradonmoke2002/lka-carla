/**:
  ros__parameters:
    weights: "/home/peeradon/lka-carla-yolo/models/best_vision.pt"
    weather_mode: "rain"

    # Minimum mean confidence to accept a detection
    min_detection_conf: 0.45

    # Polyfit lane extraction (pixel-level, all mask pixels in ROI band)
    poly_degree: 1
    min_pixels: 50
    y_top_ratio: 0.50
    y_ref_ratio: 0.85
    lane_width_m: 4.0
